server:
  port: 8080

spring:
  datasource:

    url: jdbc:mysql://localhost:3306/homestay_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 20020513wlhb
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false

  jackson:
    time-zone: Asia/Shanghai
  web:
    resources:
      static-locations: file:D:wlh/homestay_upload/  # 注意：路径建议补全为 D:/wlh/homestay_upload/（添加斜杠）
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB